# Especificar que todos los archivos de imagen son binarios
*.jpg binary
*.jpeg binary
*.png binary
*.tga binary

# Archivos de audio
*.wav binary
*.mp3 binary

# Archivos de texto
*.ini text
*.txt text
*.log text

# Archivos específicos de Unreal Engine
*.uproject text
*.uasset binary
*.umap binary
*.utxt text

# Finalización de línea automática para evitar problemas en diferentes sistemas operativos
* text=auto

# Archivos grandes que Unreal Engine usa y deben ir en Git LFS
*.uasset filter=lfs diff=lfs merge=lfs -text
*.umap filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text

